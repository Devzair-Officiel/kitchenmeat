services:
  kitchenmeat:
    build:
      context: .
      dockerfile: Dockerfile
    image: kitchenmeat:latest
    container_name: kitchenmeat
    restart: unless-stopped

    expose:
      - "80"

    networks:
      - proxy

networks:
  proxy:
    external: true
    name: proxy