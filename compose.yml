services:
  kitchenmeat:
    build:
      context: .
      dockerfile: Dockerfile
    image: kitchenmeat:latest
    container_name: kitchenmeat
    restart: unless-stopped
    ports:
      - "${PORT:-80}:80"
